{
  "name": "@hugojosefson/virsh-transient-disk",
  "version": "2.0.4",
  "license": "MIT",
  "description": "Converts a VM definition from virsh(1) to a new one with the disk marked transient.",
  "author": "Hugo Josefson <hugo@josefson.org> (https://www.hugojosefson.com/)",
  "repository": "hugojosefson/virsh-transient-disk",
  "keywords": [
    "cli",
    "virsh",
    "libvirt",
    "transient",
    "kvm",
    "qemu"
  ],
  "bin": "transient-disk.js",
  "scripts": {
    "test": "yarn lint-fix && ./test.bash",
    "lint": "standard",
    "lint-fix": "standard --fix"
  },
  "dependencies": {
    "arg": "^4.0.1",
    "get-stdin": "^8.0.0",
    "uuid": "^8.2.0",
    "xml-js": "^1.6.8"
  },
  "devDependencies": {
    "standard": "^14.3.4"
  },
  "engines": {
    "node": "^10.13.0 || >=11.10.1"
  }
}
